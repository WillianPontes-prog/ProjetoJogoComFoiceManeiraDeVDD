#include "Weapons.h"

Weapons::Weapons(float tam_x, float tam_y, float pos_x, float pos_y, float veloc, float dir, float dan, float temp,float id, float espec)
{
	tam_X = tam_x;
	tam_Y = tam_y;
	pos_X = pos_x;
	pos_Y = pos_y;
	velocidade = veloc;
	direc = dir;
	dano = dan;
	especial = espec;
	Id = id;
	tempo = temp;

}
